# Definition for a binary tree node.
# class TreeNode:
#     def __init__(self, x):
#         self.val = x
#         self.left = None
#         self.right = None

class Solution:
    def searchBST(self, root, val):
	thislevel = [root]
	while root.val != val:
		if root.left
		thislevel.append()
	if root.val == val:
		return this.val, this.left, this.right
	else:
		
